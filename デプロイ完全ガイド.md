# 完全デプロイガイド: Cloudflare Pages + AWS

このガイドでは、Cloudflare Pages（フロントエンド）とAWS EC2（バックエンド）で公開されているアプリの、デプロイの仕組みと動作確認の手順を説明します。

## 📋 全体の流れ

1. **バックエンドAPIのデプロイ（AWS EC2、GitHub Actions経由で自動）**
2. **Cloudflare Pagesでフロントエンドをデプロイ**
3. **環境変数を設定して接続**
4. **動作確認**

---

## パート1: バックエンドAPI（AWS EC2）

バックエンドは以前はFly.ioで手動デプロイしていましたが、現在はAWS EC2上でsystemdサービスとして常駐し、
`master`ブランチへのpushをトリガーにGitHub Actions（`.github/workflows/deploy-backend.yml`）が
自動でデプロイします。`flyctl`のようなCLIツールをローカルで実行する必要はありません。

### デプロイの仕組み

1. `backend/**`配下、またはワークフロー自体に変更があるコミットが`master`にpushされる
2. `backup-db.yml`が先にSSM経由でEC2上のSQLiteデータベースをバックアップし、S3に保存
3. `deploy-backend.yml`がAWS SSM（`aws ssm send-command`）経由でEC2インスタンスに接続し、以下を実行
   - `git fetch && git reset --hard origin/master`
   - `cargo build --release`
   - `sudo systemctl restart live-schedule-api`

認証はGitHub ActionsのOIDCによるAWS IAMロール引き受け（`AWS_ROLE_ARN`）で行われ、
長期的なアクセスキーは使用していません。

### 必要なGitHub Secrets/Variables

- `AWS_ROLE_ARN` - デプロイ用IAMロールのARN
- `AWS_REGION` - AWSリージョン（未設定時は`ap-northeast-1`）
- `EC2_INSTANCE_ID` - 対象EC2インスタンスID
- `AWS_BACKUP_BUCKET`（リポジトリ変数） - バックアップ先S3バケット名

### 手動でデプロイをトリガーする場合

GitHubリポジトリの「Actions」タブ → 「Deploy Backend (AWS EC2)」→「Run workflow」から手動実行できます。

### 動作確認

```bash
# ヘルスチェック（実際のバックエンドURLに置き換えてください）
curl https://<バックエンドの公開URL>/health
```

`{"status":"ok"}`が返ってくれば成功です。

---

## パート2: Cloudflare Pagesでフロントエンドをデプロイ

### ステップ1: Cloudflareアカウントの作成

1. https://pages.cloudflare.com にアクセス
2. 「Sign up」をクリック
3. メールアドレスまたはGitHubアカウントで登録
4. 無料プランで開始

### ステップ2: 環境変数ファイルの作成

```bash
cd frontend

# .env.productionを作成（バックエンドのAPI URLを設定）
echo "EXPO_PUBLIC_API_BASE=https://<バックエンドの公開URL>" > .env.production
```

**重要**: `<バックエンドの公開URL>`は、実際のAWS EC2側のURLに置き換えてください。

### ステップ3: Gitリポジトリの準備

#### GitHubリポジトリを作成（まだの場合）

1. GitHubにログイン
2. 右上の「+」→「New repository」をクリック
3. リポジトリ名: `live-schedule-api`（任意）
4. 「Create repository」をクリック

#### コードをプッシュ

```bash
cd /Users/mei/workspace/live-schedule-api

# Gitリポジトリを初期化（まだの場合）
git init

# リモートリポジトリを追加（your-usernameを実際のGitHubユーザー名に置き換え）
git remote add origin https://github.com/your-username/live-schedule-api.git

# コミット
git add .
git commit -m "Initial commit for deployment"

# プッシュ
git push -u origin master
```

### ステップ4: Cloudflare Pagesでプロジェクトを作成

1. Cloudflareダッシュボードにログイン
2. 左メニューから「Workers & Pages」を選択
3. 「Pages」タブをクリック
4. 「Create a project」をクリック
5. 「Connect to Git」を選択
6. GitHubアカウントを連携（初回のみ、認証を許可）
7. リポジトリ `live-schedule-api` を選択
8. 「Begin setup」をクリック

### ステップ5: ビルド設定

以下の設定を入力：

- **Project name**: `live-schedule`（任意）
- **Production branch**: `master`（または`main`）
- **Framework preset**: `None`を選択
- **Build command**: 
  ```
  cd frontend && npm install && npm run build
  ```
- **Build output directory**: 
  ```
  frontend/web-build
  ```

### ステップ6: 環境変数の設定

1. 「Environment variables」セクションを開く
2. 「Add variable」をクリック
3. 以下を追加：
   - **Variable name**: `EXPO_PUBLIC_API_BASE`
   - **Value**: `https://<バックエンドの公開URL>`（AWS EC2側のURL）
4. 「Save and Deploy」をクリック

### ステップ7: デプロイの完了を待つ

ビルドとデプロイが自動的に開始されます。完了まで5-10分かかります。

### ステップ8: デプロイURLを確認

デプロイ完了後、以下のようなURLが発行されます：
- `https://live-schedule-xxxxx.pages.dev`

このURLをメモしておいてください。

---

## パート3: 環境変数の最終設定

### バックエンドの環境変数を更新

Cloudflare PagesのURLが確定したら、EC2インスタンス上の環境変数（`BASE_URL`、`ALLOWED_ORIGIN`）を
Cloudflare PagesのURLに更新し、`live-schedule-api`サービスを再起動してください。
環境変数はEC2インスタンス上のサービス設定（EnvironmentFileまたはsystemdユニット）で管理されています。
AWS SSM経由でリモート実行する場合は、`.github/workflows/deploy-backend.yml`と同様に
`aws ssm send-command`で対象EC2インスタンスに対して設定変更・`sudo systemctl restart live-schedule-api`を実行します。

---

## パート4: 動作確認

### 1. フロントエンドの確認

ブラウザで以下にアクセス：
- `https://live-schedule-xxxxx.pages.dev`

### 2. API接続の確認

ブラウザの開発者ツール（F12）を開き、「Console」タブでエラーがないか確認。

### 3. ログイン機能のテスト

1. フロントエンドで新規登録を試す
2. ログインを試す
3. スケジュールの作成・表示を試す

---

## トラブルシューティング

### バックエンド（AWS EC2）関連

#### デプロイが失敗する

1. GitHubリポジトリの「Actions」タブで「Deploy Backend (AWS EC2)」の実行ログを確認
2. `aws ssm get-command-invocation`の出力（`StandardOutputContent`/`StandardErrorContent`）でビルドエラーを確認
3. ローカルでビルドをテスト：
   ```bash
   cd backend
   cargo build --release
   ```

#### データベースエラー

- `live-schedule-api`サービスが起動しているか確認（`systemctl status live-schedule-api`をSSM経由で実行）
- 直近のバックアップはS3（`s3://<AWS_BACKUP_BUCKET>/database/YYYY/MM/`）から取得可能

### Cloudflare Pages関連

#### ビルドが失敗する

1. Cloudflare Pagesの「Deployments」タブでログを確認
2. ローカルでビルドをテスト：
   ```bash
   cd frontend
   npm install
   npm run build
   ```

#### 環境変数が反映されない

1. 環境変数を設定後、再デプロイが必要
2. 「Deployments」タブで「Retry deployment」をクリック

#### API接続エラー（CORSエラー）

1. バックエンドの`ALLOWED_ORIGIN`が正しく設定されているか確認
2. ブラウザのコンソールでエラーメッセージを確認

---

## 次のステップ

### カスタムドメインの設定（オプション）

#### Cloudflare Pagesでカスタムドメイン

1. Cloudflare Pagesのプロジェクト設定を開く
2. 「Custom domains」タブをクリック
3. 「Set up a custom domain」をクリック
4. 使用するドメインを入力
5. DNS設定の指示に従う

#### バックエンド（AWS）でカスタムドメイン

Route53でのDNS設定、ACM証明書の発行、またはEC2上のリバースプロキシ（Nginx等）でのTLS終端など、
実際のインフラ構成に応じて設定してください。

---

## まとめ

- **フロントエンド**: Cloudflare Pages（無料、高速CDN付き）
- **バックエンド**: AWS EC2（GitHub Actions経由で自動デプロイ）

問題があれば、GitHub Actionsの実行ログを確認してトラブルシューティングしてください。

















